# Streamlit Community Cloud reads THIS file (app/requirements.txt).
# Minimal runtime deps — the app reads precomputed artifacts, no GEE/geo stack.
# Lower-bound pins (not exact) so a prebuilt wheel is available on whatever
# Python version Streamlit Cloud provisions (3.12–3.14), avoiding slow/failed
# source builds of numpy/pyarrow.
streamlit>=1.40
pandas>=2.2
numpy>=1.26
plotly>=5.24
pyarrow>=17.0
